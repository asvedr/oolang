exception EInt : int;

fn main() : () {
	try {
		var a = readln() as int;
		var b = readln() as int;
		var c = [1,2,3,4];
		print((a + b + c.len()) as str);
		throw EInt 5;
		throw NullPtr;
	} catch EInt a {
		print("error found")
	}
}
