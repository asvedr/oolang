class single Main {
	pub a : real;
	pub fn curry_add(a : int) : Fn(real):int {
		fn closure(b : real) : int {
			return a + b as int;
		};
		return closure
	};
	priv fn add(p : int) {
		self.a = self.a + p;
	};
}
